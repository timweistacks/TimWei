# Changelog

All notable changes to this project will be documented in this file.

## [1.2.0] - 2026-05-24
### Added
- **最新動態單卡輪播**：在首頁最新動態（Live Activity）中實現單卡輪動播放，配有向上滑入與漸進淡入 (`slideFadeIn` 動畫)、金沙奶茶色左側邊框等直播感視覺，並以 3 秒為週期進行焦點輪替，且保留高度的響應式自適應。
- **正負號格式化**：未實現損益與 realized PnL 盈虧金額在正數時顯式顯示 `+` 號，負數顯示 `-` 號。

### Changed
- **品牌用詞統一為「報酬疊加」**：
  - 首頁及明細頁 `<title>`、頂部導覽列 `nav.title` 統一改為 **`Tim Wei 報酬疊加實驗`**。
  - 曝險拆解面板標題統一改為 **`報酬疊加策略拆解`**。
  - FB 社群加入連結中英文翻譯統一改為 **`加入 FB 報酬疊加交流社團`** (en: `Join FB Return Stacking Group`)。
- **全站紅漲綠跌**：不分中英文語系，將全站的上漲與獲利狀態（`good`、`is-up`）改為紅色，下跌與虧損狀態（`bad`、`is-down`）改為綠色，符合台灣交易習慣；同時確保系統錯誤提示（`.overview-error` 等）維持代表警示的紅色。
- **移除導覽列 LIVE 徽章**：將導覽列中的 `live-badge-wrap` 徹底移除，避免視覺冗餘。

---

## [1.1.0] - 2026-05-24
### Added
- 在明細頁的「成交紀錄」中，新增高質感「交易時間軸 (Timeline)」視圖模式，並附帶「表格/時間軸」點擊切換器。
- 時間軸中新增決策備忘錄區塊，讀取並高亮呈現交易資料中的 `note` 欄位（決策理由），且保證無 note 時的向後相容。
- 新增 `locale.js` 時間軸與切換按鈕對應的英中文翻譯 Key。
- 在 `test_ui_fix.py` 中為時間軸與切換器 DOM 元素加入單元測試，且測試全部通過。

### Changed
- 修改 Return Stacking 策略拆解說明，將 `viz.desc_3d` 中文描述改為「此配置由Tim Wei 自製搭建」，更能反映自製搭配語意。
- 將 `details.html`、`app.js`、`styles.css` 和 `locale.js` 更新版本後綴為 `?v=5`。

---

## [1.0.0] - 歷史優化版本
### Added
- 首頁右下角新增毛玻璃質感社群交流卡片，突出「FB 槓桿社團」大按鈕與 Threads、IG 連結。
- 組合 NAV 折線圖優化：Y 軸 title 精簡為「區間指數」，X 軸時間 ticks 限制最少 5 個且格式化為 `MM/DD`，增加 `grace: "2%"` 呼吸空間，且「我的 NAV」主線加粗置於最上層。
- 折線圖主線終點新增奶茶色 3D 呼吸光暈動畫點。
- 偏離度天平 `% 偏離` 數值套用高對比深綠/深藍/磚紅/琥珀橘文字著色，並移除背景膠囊底色，提升辨識度.
- KPI 小格子奇數跨欄填滿優化。
- 明細頁次導覽列 `.detail-switcher` `sticky` 定位優化，主導覽列改為隨滾動移出。
